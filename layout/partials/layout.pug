doctype html
html(lang=config.language)
  head
    include head
  body(class= is_home() ? `home` : is_post() ? `post` : null)
    include ./header

    include ./container

    include ./footer
    include ./foot